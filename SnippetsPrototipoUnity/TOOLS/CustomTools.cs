using UnityEngine;
//using UnityEditor;
using System.Collections;
using System.Collections.Generic;

public class CustomTools //: EditorWindow
{
/*
    public static void Init()
    {
        //List<GameObject> gameObjects = new List<GameObject>();
        //gameObjects.Add(GameObject.Find("PREFAB_Mureta_A 3"));
        //Selection.objects = gameObjects.ToArray();
        GameObject[] objects = Selection.gameObjects;

        if (objects.Length > 0)
        {
            for (int i = objects.Length - 1; i >= 0; i--)
            {
                Vector3 position = objects[i].transform.position;
                DestroyImmediate(objects[i]);
                GameObject cube = GameObject.CreatePrimitive(PrimitiveType.Cube);
                cube.transform.position = position;
            }
        }
    }

    [MenuItem("Custom/Select direct parents %+")]
    public static void SelectParents()
    {
        GameObject[] objs = Selection.gameObjects;
        List<GameObject> parents = new List<GameObject>();
        foreach (GameObject obj in objs)
        {
            if (obj.transform.parent != null)
            {
                parents.Add(obj.transform.parent.gameObject);
            }
        }
        Selection.objects = parents.ToArray();
    }
    */
}

